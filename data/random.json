<script>
async function goRandomCountry() {
  try {
    const res = await fetch("/data/countries.json?nocache=" + Date.now());
    const data = await res.json();

    const keys = Object.keys(data);
    if (!keys.length) {
      alert("No countries found");
      return;
    }

    const randomKey = keys[Math.floor(Math.random() * keys.length)];

    const lg = (localStorage.getItem("wigg_lang") || "en").toLowerCase();
    const ul = ["en","fr","es"].includes(lg) ? lg : "en";
    window.location.href = "/countries/" + randomKey + "-" + ul + ".html";

  } catch (e) {
    console.error("Random country error:", e);
    alert("Random failed");
  }
}
</script>
